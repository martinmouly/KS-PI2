const ConnectMetamask = ({ props }) => {
  const { testProps } = props;
  let state = () => {};
  const connectHandler = () => {};

  return <button onClick={connectHandler}>{this.state.buttonText}</button>;
};

<ConnectMetamask color={blue} />;
